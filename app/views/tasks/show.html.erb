<h1>Task details</h1>
<p><%= @task.title %></p>
<p><%= @task.details %></p>
<% if @task.completed  %>
  <p>done</p>
  <% else %>
  <p>need to work on it </p>
  <% end %>

<%= link_to("Back to list", tasks_path) %>
